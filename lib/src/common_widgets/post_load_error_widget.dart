import 'package:flutter/material.dart';

class PostLoadErrorWidget extends StatelessWidget {
  const PostLoadErrorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
